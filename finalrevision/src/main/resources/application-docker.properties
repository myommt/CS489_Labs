spring.docker.compose.enabled=true
spring.docker.compose.lifecycle-management=start-only

# When running with Docker Compose, connect via localhost to the published port
spring.datasource.url=jdbc:mysql://localhost:3306/srmdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:RootAbcde12345!}